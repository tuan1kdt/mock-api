name = "mock-api-frontend"
compatibility_date = "2025-11-22"

# Cloudflare Pages configuration
pages_build_output_dir = ".vercel/output/static"

# Custom domains are configured via Cloudflare Dashboard, not in this file.
# To set a custom domain:
# 1. Go to Cloudflare Dashboard > Workers & Pages > Your Project > Custom domains
# 2. Click "Set up a custom domain" and enter your domain
# 3. Follow the DNS setup instructions

[vars]
# Production environment variables
# For local development, use .dev.vars file (gitignored)
# These are production defaults - override in Cloudflare Dashboard if needed
BACKEND_URL = "https://mock-api-backend.your-subdomain.workers.dev"

